#pragma once
#include "RegPDFA.h"
class RegPDFAGenerator
{
public:
   // this class enumerates all regdpfa with states being indexed by binary strings.
   // alphabet is {0,1}.
   // the dfas are represented as strings.

   RegPDFAGenerator();

   struct Promise
   {
      string str;

   };

private:
   
};

